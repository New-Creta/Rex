#!/usr/bin/env python                                                                    
import os
import regis.util

def main():
  root = regis.util.find_root()
  os.system(f"py {os.path.join(root, '_setup.py')}")

if __name__ == "__main__":
  main()