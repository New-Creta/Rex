#pragma once

namespace rex
{
    namespace renderer
    {
        enum class InputLayoutClassification
        {
            NONE = 0,
            PER_VERTEX_DATA,
            PER_INSTANCE_DATA
        };
    }
}