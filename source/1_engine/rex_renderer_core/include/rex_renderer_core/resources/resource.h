#pragma once

namespace rex
{
  namespace rhi
  {
    // The resource class just acts as an base class for all gpu resources
    class Resource
    {
    public:
      virtual ~Resource() = default;

    private:

    };
  }
}